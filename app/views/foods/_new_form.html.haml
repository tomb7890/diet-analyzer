= form_tag new_day_food_path, :class => "form-inline", :method => :get do
  %div{:class => 'form-group'}
    = label_tag "search", "Search: "
    = text_field_tag "search", params[:search], class: "form-control", placeholder: "e.g. Strawberries"
    = submit_tag "Submit", class: "form-control"
